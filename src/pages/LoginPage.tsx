import LoginForm from '../components/LoginForm';

const LoginPage = () => {
  return (
    <main className="min-h-full w-full flex items-center flex-col bg-neutral-0 justify-center py-8">
      <LoginForm />
    </main>
  );
};

export default LoginPage;
